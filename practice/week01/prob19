모든 글자가 소문자인 경우 대문자로, 모든 글자가 대문자인 경우 소문자로 바꾸는 함수를 작성하시오

        let x = prompt("소대문자 체인지","the weekend");
        function change (){
            if (x == x.toLowerCase()){ //소문자 케이스
                let str = x.toUpperCase();
                document.write(str);    
            }
            else if(x == x.toUpperCase()){ //대문자케이스
                let str = x.toLowerCase();
                document.write(str);
            }
            else{ //모든 글자가 통일되지 않음
                document.write("모든 글자가 소문자 또는 대문자가 아닙니다.")
            }
        } 
        change(x);
